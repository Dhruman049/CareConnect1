<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css"> 
    <title>Patient Dashboard</title>
</head>
<body>
    <% if (!role) { %>
        <%- include('home-navbar') %>
    <% } else { %> 
        <%- include(role==='Admin' ? 'admin-navbar' : role==='Specialist' ? 'specialist-navbar' : 'patient-navbar' ) %>
        <% } %>
  
    <!-- Dashboard Container -->
    <div class="dashboard-container">
        <h1>Welcome to Your Dashboard!</h1>
       
        <p class="user-name">Hello, <strong><%= userName %></strong></p>
        
        <div class="dashboard-cards">
            <div class="dashboard-card">
                <h2>Manage Appointments</h2>
                <p>Schedule, reschedule, or cancel appointments with your healthcare provider.</p>
                <a href="/appointments" class="btn">Go to Appointments</a>
            </div>

            <div class="dashboard-card">
                <h2>View Lab Reports</h2>
                <p>Access your latest medical test results and reports securely.</p>
                <a href="/patient/lab-reports" class="btn">View Reports</a>
            </div>

            <div class="dashboard-card">
                <h2>Consult a Specialist</h2>
                <p>Find and book consultations with specialists based on your medical needs.</p>
                <a href="/consult-specialist" class="btn">Find a Specialist</a>
            </div>
        </div>
    </div>

    <%- include('footer') %>
</body>
</html>